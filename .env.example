# Gemini API key from https://aistudio.google.com.
# This is consumed ONLY by api/chat.ts (a Vercel Function). It is NOT prefixed
# with VITE_, so Vite will not inline it into the browser bundle.
GEMINI_API_KEY=
